The directory /node/root is always present.
It represents the top-level subtrees of this doctree.

The first subtree is specified in head.json.

Note that if you want to include the whole doctree somewhere else in the
doctree, you simply reference "node":"root".
